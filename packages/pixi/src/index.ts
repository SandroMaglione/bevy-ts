/**
 * Pixi-specific integrations live in this package.
 *
 * Public APIs added here must stay renderer-specific and avoid feature-level
 * opinions so they remain composable across games.
 */
export const packageTag = "pixi" as const
